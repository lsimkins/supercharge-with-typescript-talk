function concatenate(param1: string, param2: string): string {
  return param1 + param2;
}

// returns 'javascript-typescript'
concatenate('javascript', '-typescript');

concatenate('javascript');
concatenate('javascript', 123);
